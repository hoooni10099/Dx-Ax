#include <stdio.h>

int main()
{
    int a = 10, b = 20;
    // int temp;

    printf("교환 전 a와 b의 값 : %d, %d\n", a, b);
    
    {
        int temp;

        temp = a;
        a = b;
        b = temp;

        // printf("temp : %d\n", temp);
    }

    // printf("temp : %d\n", temp);
    printf("교환 후의 a와 b의 값 : %d, %d\n", a, b);

    return 0;
}
