#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main()
{
    srand(time(NULL)); //NULL-> 0
    
    // int num = rand() % 100 + 1;
    // printf("Random number: %d\n", num);

    for(int i = 0; i < 6; i++) {
        printf("%d ", rand() % 45 + 1);
    }
    printf("\n");

    return 0;
}
